// This file is deprecated. Use mobs/MobTypes.ts instead.
//
// Contains AI-generated edits.

import Phaser from 'phaser';
import { Mob, MobConfig } from '../entities/Mob';

export class NormalLetterMob extends Mob {
  constructor(scene: Phaser.Scene, x: number, y: number, config: MobConfig) {
    super(scene, x, y, config);
  }

  onTyped(letter: string): boolean {
    return super.onTyped(letter);
  }
}

export class TankWordMob extends Mob {
  constructor(scene: Phaser.Scene, x: number, y: number, config: MobConfig) {
    super(scene, x, y, config);
  }

  onTyped(letter: string): boolean {
    return super.onTyped(letter);
  }
}

export class ArmoredLetterMob extends Mob {
  constructor(scene: Phaser.Scene, x: number, y: number, config: MobConfig) {
    super(scene, x, y, config);
  }

  onTyped(letter: string): boolean {
    return super.onTyped(letter);
  }
}

export class ShieldedWordMob extends Mob {
  constructor(scene: Phaser.Scene, x: number, y: number, config: MobConfig) {
    super(scene, x, y, config);
  }

  onTyped(letter: string): boolean {
    return super.onTyped(letter);
  }
}

export class RegeneratorMob extends Mob {
  constructor(scene: Phaser.Scene, x: number, y: number, config: MobConfig) {
    super(scene, x, y, config);
  }

  onTyped(letter: string): boolean {
    return super.onTyped(letter);
  }
}

export class SplitWordMob extends Mob {
  constructor(scene: Phaser.Scene, x: number, y: number, config: MobConfig) {
    super(scene, x, y, config);
  }

  onTyped(letter: string): boolean {
    return super.onTyped(letter);
  }
}

export class StealthLetterMob extends Mob {
  constructor(scene: Phaser.Scene, x: number, y: number, config: MobConfig) {
    super(scene, x, y, config);
  }

  onTyped(letter: string): boolean {
    return super.onTyped(letter);
  }
}

export class SpeedsterMob extends Mob {
  constructor(scene: Phaser.Scene, x: number, y: number, config: MobConfig) {
    super(scene, x, y, config);
  }

  onTyped(letter: string): boolean {
    return super.onTyped(letter);
  }
}

export class BossMob extends Mob {
  constructor(scene: Phaser.Scene, x: number, y: number, config: MobConfig) {
    super(scene, x, y, config);
  }

  onTyped(letter: string): boolean {
    return super.onTyped(letter);
  }
}
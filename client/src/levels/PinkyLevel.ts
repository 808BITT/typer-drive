import { LevelConfig } from '@/types';

export const PinkyLevelConfig: LevelConfig = {
    id: 'pinky-1',
    name: 'Pinky Finger: All Rows',
    // Expanded to include all pinky finger letters
    letters: ['A', 'Q', 'Z', ';', 'P', "'"],
    boss: false
};